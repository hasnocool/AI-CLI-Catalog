# Contributing

When adding or changing a CLI entry:

1. Prefer the vendor's official documentation or upstream repository.
2. Record the shortest supported installation command for Linux/macOS when available.
3. Keep `README.md` and `catalog.json` synchronized.
4. Do not add third-party mirror installers when an official installer exists.
5. Review remote shell scripts before recommending them.
